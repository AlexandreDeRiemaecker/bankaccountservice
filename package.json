{
  "private": true,
  "workspaces": [
    "infrastructure"
  ],
  "scripts": {
    "deploy": "yarn --cwd ./infrastructure run deploy",
    "hotdeploy": "yarn --cwd ./infrastructure run hotdeploy"
  }
}
